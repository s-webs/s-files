# S-Files - Универсальный файловый менеджер для Laravel

Современный, универсальный файловый менеджер для Laravel с красивым интерфейсом, поддержкой drag & drop, предпросмотром файлов и множеством других функций.

> **Примечание:** Для английской документации см. [README.md](README.md)

## Особенности

- 🎨 Современный интерфейс на Alpine.js и Tailwind CSS
- 📁 Управление файлами и папками
- 📤 Загрузка файлов через drag & drop
- 👁️ Предпросмотр изображений, PDF и документов
- 🔒 Опциональная авторизация (можно отключить)
- 🚀 Rate limiting для защиты от злоупотреблений
- 🔐 Безопасная валидация путей и файлов
- 📦 Поддержка различных storage дисков
- 🎯 Полностью настраиваемый

## Требования

- PHP >= 8.2
- Laravel >= 10.0 или >= 11.0
- Node.js >= 18.0 и npm (для сборки assets)
- Vite (для сборки frontend-ресурсов)

## Установка

### Через Composer

```bash
composer require s-webs/s-files
```

### Публикация конфигурации

```bash
php artisan vendor:publish --tag=sfiles-config
```

### Публикация views (опционально, для кастомизации)

```bash
php artisan vendor:publish --tag=sfiles-views
```

### Публикация assets (опционально, для кастомизации)

```bash
php artisan vendor:publish --tag=sfiles-assets
```

### Установка npm-зависимостей

Пакет требует следующие npm-зависимости:
- `alpinejs` (^3.15.4) - для реактивности интерфейса
- `dropzone` (^6.0.0-beta.2) - для drag & drop загрузки файлов
- `compressorjs` (^1.2.1) - для сжатия изображений перед загрузкой

**Вариант 1: Установка в корневой проект (рекомендуется)**

Если вы используете пакет в своем проекте, установите зависимости в корневом `package.json`:

```bash
npm install alpinejs@^3.15.4 dropzone@^6.0.0-beta.2 compressorjs@^1.2.1
```

**Вариант 2: Установка в пакете (для разработки)**

Если вы разрабатываете пакет локально:

```bash
cd vendor/s-webs/s-files
npm install
```

### Настройка Vite

Для работы пакета необходимо настроить Vite. Добавьте файлы пакета в `vite.config.js`:

```js
import { defineConfig } from 'vite';
import laravel from 'laravel-vite-plugin';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
    plugins: [
        laravel({
            input: [
                'resources/css/app.css',
                'resources/js/app.js',
                // Добавьте эти строки для работы пакета
                'packages/s-webs/s-files/resources/css/filemanager.css',
                'packages/s-webs/s-files/resources/js/filemanager.js',
            ],
            refresh: true,
        }),
        tailwindcss(),
    ],
});
```

**Примечание:** Если вы опубликовали assets через `vendor:publish`, используйте пути:
- `resources/css/vendor/sfiles/filemanager.css`
- `resources/js/vendor/sfiles/filemanager.js`

### Сборка assets

После настройки Vite, соберите assets:

```bash
# Для разработки (с hot reload)
npm run dev

# Для продакшена
npm run build
```

**Важно:** Убедитесь, что Vite dev server запущен (`npm run dev`) при разработке, или выполните сборку (`npm run build`) для продакшена.

## Настройка

### 1. Конфигурация

Откройте файл `config/sfiles.php` и настройте параметры:

```php
// Storage disk для файлов
'disk' => env('SFILES_DISK', 'uploads'),

// Префикс публичной директории
'public_dir' => env('SFILES_PUBLIC_DIR', 'uploads'),

// Префикс маршрутов
'routes' => [
    'prefix' => env('SFILES_ROUTE_PREFIX', 's-files'),
    'middleware' => ['web'],
],

// Авторизация (опциональная)
'auth' => [
    'enabled' => env('SFILES_AUTH_ENABLED', false), // false = без авторизации
    'middleware' => env('SFILES_AUTH_MIDDLEWARE', 'auth'),
],
```

### 2. Настройка Storage

Убедитесь, что в `config/filesystems.php` настроен диск для файлов:

```php
'disks' => [
    'uploads' => [
        'driver' => 'local',
        'root' => storage_path('app/uploads'),
        'url' => env('APP_URL').'/uploads',
        'visibility' => 'public',
    ],
],
```

И создайте символическую ссылку:

```bash
php artisan storage:link
```

### 3. Переменные окружения (.env)

```env
# Storage disk
SFILES_DISK=uploads

# Публичная директория
SFILES_PUBLIC_DIR=uploads

# Префикс маршрутов
SFILES_ROUTE_PREFIX=s-files

# Авторизация (true/false)
SFILES_AUTH_ENABLED=false

# Middleware для авторизации (если enabled = true)
SFILES_AUTH_MIDDLEWARE=auth

# Максимальный размер файла в KB (по умолчанию 10MB)
SFILES_MAX_SIZE=10240

# Rate limiting
SFILES_RATE_LIMIT_UPLOAD=100
SFILES_RATE_LIMIT_DELETE=30
SFILES_RATE_LIMIT_GENERAL=60
```

## Использование

### Базовое использование

После установки файловый менеджер будет доступен по адресу:

```
http://your-app.test/s-files
```

Или по вашему настроенному префиксу.

### С авторизацией

Если вы хотите защитить файловый менеджер авторизацией:

1. Установите `SFILES_AUTH_ENABLED=true` в `.env`
2. Настройте `SFILES_AUTH_MIDDLEWARE` (например, `auth` для стандартной Laravel авторизации)

### Без авторизации

По умолчанию авторизация отключена (`SFILES_AUTH_ENABLED=false`). Файловый менеджер будет доступен всем.

**⚠️ Внимание:** Используйте без авторизации только в безопасных окружениях или защитите маршруты на уровне веб-сервера.

### Интеграция с MoonShine

Если вы используете MoonShine и хотите интегрировать файловый менеджер:

1. Создайте кастомный middleware для MoonShine авторизации
2. Установите `SFILES_AUTH_ENABLED=true`
3. Настройте `SFILES_AUTH_MIDDLEWARE` на ваш кастомный middleware

Пример middleware:

```php
<?php

namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;
use MoonShine\Laravel\MoonShineAuth;

class MoonShineFileManagerAuth
{
    public function handle(Request $request, Closure $next)
    {
        if (!MoonShineAuth::getGuard()->check()) {
            abort(401);
        }
        
        return $next($request);
    }
}
```

Затем в `config/sfiles.php`:

```php
'auth' => [
    'enabled' => true,
    'middleware' => \App\Http\Middleware\MoonShineFileManagerAuth::class,
],
```

## API Endpoints

Все endpoints доступны через префикс, указанный в конфигурации:

- `GET /s-files` - Главная страница файлового менеджера
- `GET /s-files/files?path=` - Получить список файлов и папок
- `POST /s-files/upload` - Загрузить файл
- `POST /s-files/create-folder` - Создать папку
- `POST /s-files/delete` - Удалить файл
- `POST /s-files/delete-folder` - Удалить папку
- `POST /s-files/rename` - Переименовать файл/папку
- `GET /s-files/download-folder?path=` - Скачать папку как ZIP
- `POST /s-files/download-files` - Скачать выбранные файлы как ZIP

## Безопасность

Пакет включает множество мер безопасности:

- ✅ Защита от path traversal атак
- ✅ Валидация MIME типов и расширений файлов
- ✅ Проверка сигнатур файлов (для Office документов)
- ✅ Rate limiting для защиты от злоупотреблений
- ✅ Санитизация имен файлов
- ✅ Ограничение глубины путей
- ✅ Блокировка опасных расширений

## Кастомизация

### Views

После публикации views (`php artisan vendor:publish --tag=sfiles-views`), вы можете кастомизировать их в `resources/views/vendor/sfiles/`.

### Assets

После публикации assets (`php artisan vendor:publish --tag=sfiles-assets`), вы можете кастомизировать CSS и JavaScript в `resources/css/vendor/sfiles/` и `resources/js/vendor/sfiles/`.

**Важно:** После публикации assets убедитесь, что:
1. Установлены npm-зависимости (`npm install alpinejs@^3.15.4 dropzone@^6.0.0-beta.2 compressorjs@^1.2.1`)
2. Файлы добавлены в `vite.config.js`:
   ```js
   input: [
       // ... другие файлы
       'resources/css/vendor/sfiles/filemanager.css',
       'resources/js/vendor/sfiles/filemanager.js',
   ]
   ```
3. Выполнена сборка (`npm run build`) или запущен dev server (`npm run dev`)

### Конфигурация

Все параметры можно настроить в `config/sfiles.php`:

- Разрешенные MIME типы
- Разрешенные расширения
- Заблокированные расширения
- Максимальный размер файла
- Настройки rate limiting
- Настройки кэширования
- Настройки логирования

## Лицензия

MIT License

## Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории пакета.

## Автор

S-WEBS
